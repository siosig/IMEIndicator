<Window x:Class="IMEIndicatorClock.Views.LanguageColorsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:IMEIndicatorClock.Views"
        Title="言語別色設定"
        Width="600" Height="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="500" MinHeight="400">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー説明 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="LblDialogDescription" TextWrapping="Wrap" Margin="0,0,0,10"/>
            
            <!-- グローバルフォント設定 -->
            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="LblGlobalFont" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox x:Name="GlobalFontCombo" Grid.Column="1" SelectionChanged="GlobalFont_SelectionChanged"/>
                    
                    <TextBlock x:Name="LblGlobalFontSize" Grid.Column="2" VerticalAlignment="Center" Margin="15,0,10,0"/>
                    <ComboBox x:Name="GlobalFontSizeCombo" Grid.Column="3" SelectionChanged="GlobalFontSize_SelectionChanged">
                        <ComboBoxItem Content="20%" Tag="0.2"/>
                        <ComboBoxItem Content="30%" Tag="0.3"/>
                        <ComboBoxItem Content="40%" Tag="0.4"/>
                        <ComboBoxItem Content="50%" Tag="0.5" IsSelected="True"/>
                        <ComboBoxItem Content="60%" Tag="0.6"/>
                        <ComboBoxItem Content="70%" Tag="0.7"/>
                        <ComboBoxItem Content="80%" Tag="0.8"/>
                    </ComboBox>
                </Grid>
            </Border>
            
            <!-- 列ヘッダー -->
            <Grid Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="55"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="LblColLang" FontWeight="SemiBold" Opacity="0.7"/>
                <TextBlock x:Name="LblColFont" Grid.Column="1" FontWeight="SemiBold" Opacity="0.7"/>
                <TextBlock x:Name="LblColSize" Grid.Column="2" FontWeight="SemiBold" Opacity="0.7"/>
                <TextBlock x:Name="LblColColor" Grid.Column="3" FontWeight="SemiBold" Opacity="0.7" Margin="10,0,0,0"/>
                <TextBlock x:Name="LblColText" Grid.Column="4" FontWeight="SemiBold" Opacity="0.7" HorizontalAlignment="Center"/>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="LanguageList"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnOK" Width="80" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="BtnCancel" Width="80" Margin="10,0,0,0" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
